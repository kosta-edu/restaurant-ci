sudo: required
language: java
install: true

services:
  - docker

before_install:
  - echo "start Creating an image with dockerfile"
  - docker build -t existmaser/restaurant-rest .

script:
  - echo "testing.."
  - chmod +x ./gradlew
  - ./gradlew test

after_success:
  - echo "Test Success"